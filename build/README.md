Place Windows build assets here.

- Optional app icon path for electron-builder: `build/icon.ico`
- If `build/icon.ico` is missing, packaging still works and electron-builder uses its default icon.
- Replace the placeholder with a real Windows `.ico` before creating a polished installer.
